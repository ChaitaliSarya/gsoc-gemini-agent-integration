{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run demo",
            "type": "shell",
            "command": "${workspaceFolder}/.venv/bin/python src/runner.py"
        }
    ]
}