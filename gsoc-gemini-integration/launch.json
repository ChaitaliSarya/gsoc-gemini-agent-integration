{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run runner",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/runner.py",
            "console": "integratedTerminal"
        }
    ]
}